const express = require("express");

const router = express.Router();

// v1 route modules will be mounted here as features are added
// Example: router.use("/auth", require("./v1/authRoutes"));

module.exports = router;
