// src/routes/notifications.routes.js
// Placeholder route file for notifications module – currently no endpoints are defined.
// This prevents MODULE_NOT_FOUND errors when the main index router loads.

const express = require('express');
const router = express.Router();

// Example stub endpoint (optional)
// router.get('/', (req, res) => res.json({ message: 'Notifications endpoint placeholder' }));

module.exports = router;
